# tuples in memory and accessing elements

# accessing elements in a tuple:

tuple1 = ("a", "b", "c", "d", "e")
print(tuple1[1]) # access the same way as we do a list
print(tuple1[1:3]) # ('b', 'c')

# tuple1[1] = "F" this will throw an error since immutable